CREATE TABLE Films
(
  name INT NOT NULL,
  genre INT NOT NULL,
  rating INT NOT NULL,
  releaseDate INT NOT NULL,
  removalDate INT NOT NULL,
  releaseYear INT NOT NULL,
  movieId INT NOT NULL,
  PRIMARY KEY (movieId)
);